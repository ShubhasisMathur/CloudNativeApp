apiVersion: v1
kind: Service
metadata:
    name: shubhasismathurcloudnativeapp
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: shubhasismathurcloudnativeapp